name: PowerShellTest

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: exec
      uses: actions/checkout@v3
    - run: ls -l
    - run: ls -l ../
    - run: ls -l ../../
    - run: ls -l $HOME\work\ActionsTestSimpleSample