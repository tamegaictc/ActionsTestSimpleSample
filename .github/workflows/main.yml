name: Sample
on:
  push:

jobs:
  Build1:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: make
      run: make

  Build2:
    runs-on: windows-latest
    needs: Build1
    steps:
    - name: exec
      run: ./SimpleSample.exe